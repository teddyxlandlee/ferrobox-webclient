{
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  },
  "scripts": {
    "build": "vite build && tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ferrobox-core": "github:teddyxlandlee/ferrobox-core"
  }
}
